export const enum HTTPStatusCodes {
  defaultError = 400,
  unauthorized = 401,
  forbidden = 403,
  defaultServerError = 500,
}
export const enum mailchimpSubscribe {
  failure = 0,
  success,
  already
}
